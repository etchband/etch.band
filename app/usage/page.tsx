import { buildMetadata } from '@cluesurf/leaf/utility/metadata'
import Page from '~/lib/frontend/components/pages/usage/Page'

export { viewport } from '@cluesurf/leaf/constant/viewport'

export const metadata = buildMetadata({
  site: 'züs',
  title: 'Terms of Usage @ züs',
})

export default Page
